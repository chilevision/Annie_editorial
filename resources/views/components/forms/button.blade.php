<div class="form-group {{ $wrapClass }}">
    <button type="{{ $type }}" name="{{ $name }}" class="btn {{ $inputClass }}" wire:click="{{ $wires }}" id="input-{{ $name }}">{{ __( $label ) }}</button>
</div>